# Logs

Using the [logging documentation](https://avl-core.readthedocs.io/en/latest/logging/logging.html) and [trace documentation](https://avl-core.readthedocs.io/en/latest/trace/trace.html) as reference:

    1. Update the class Env
        a. Always print "a" as hex
        a. Custom print "b" as int unless it's 0. Then print as "N/A"

    2. Transpose the class print to horizontal

    3. Create a Trace class and send 100 random A classes to be traced

